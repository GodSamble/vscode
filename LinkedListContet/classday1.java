/*package LinkedListContet;

public class classday1 {
    public static void main(String[] args){
        int x,y; //primate date type -int,float, double, char, boolean
        String name; //reference date type left value 에 포커스를 맞춤. 포인터 개념! (주소에 포커스를 맞추는 것임)
        //primate 물론 reference 중에서도 예외로 단일 하나 !! String만 앞에 빼서 바로 실행이 가능함 (>String name = "Hello world";) 이런 식으로
        //바로 설정이 가능함.

        //cf ) 생성자 메소드는 c언어의 함수와도 같은 개념
        //cf ) # < 이 문자는 컴파일러 지시자 
        //수업의 방향성 : 전체적인 컴퓨터 아키텍쳐에 관한 얘기와 명칭에 관한 어휘 개념을 짚어주심

        //jdk 와 jre (Virtual Mucine)의 'byte code'라는 가상세계에서 비논리적인 원칙에 의해 
        // 따라서 자바는 실행파일이 없으며 jre : r > run 실행하는 jre을 기본적으로 환경설치시에 설치해야한다.

        // 번외로 운영체제의 경우 하드웨어의 자원을 관리한다. 이 뜻은 여유있게 메모리를 잡았다고 가정했을 때
        // 메모리의 사용되지 않은 프로세서들을 관리한다.

        //exe. 파일과 / execute 실행하다 는 명확히 다르다.
        //new 메모리를 잡아주고 호출하라
        //new String("Hello world")라는 문장이 있으면 여기서 String << 아 메모리를 잡아달란 뜻이고 Hello world 얘를 출력하란 소리
        //public Test(int x, int y){
        //    this.x=x; this.y=y;
        //    name = null;
        //}
    }*/